export enum Priority {
  High = 0,
  Medium = 1,
  Low = 2,
}

export enum Status {
  New = 0,
  UnderTreatment = 1,
  Solved = 2,
}

export enum SortComplaintBy {
  ModifiedAt = "modified_at",
  CreatedAt = "created_at",
}
